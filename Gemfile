source "https://rubygems.org"

# Use github-pages to match GitHub Pages' supported gem versions
gem "github-pages", group: :jekyll_plugins

# Optional plugins: include only if your site actually uses them
gem "jekyll-seo-tag"
gem "jekyll-sitemap"

# Added to match plugins referenced in the Pages build logs
gem "jekyll-github-metadata"
gem "jekyll-theme-primer"
gem "jekyll-coffeescript"
