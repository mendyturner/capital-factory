source "https://rubygems.org"

# Use github-pages to match GitHub Pages' supported gem versions
gem "github-pages", group: :jekyll_plugins

# Add webrick for local `jekyll serve` on newer Rubies (Ruby >= 3.0)
group :development do
  gem "webrick", "~> 1.7"
end
